const todoList = [{id: 1, name: "go to gym"}];

const name = "sajan";

const environment = 'development'

const appTitle = 'Blog App';

export {todoList, name, environment};

export default appTitle;

//no muttiple default